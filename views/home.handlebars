<h1>Welcome to the Tech Blog!</h1>
{{#each posts as |post|}}
    <div class="post">
        <h2>{{post.title}}</h2>
        <p>{{post.content}}</p>
        <p>Posted by {{post.user.username}} on {{format_date post.createdAt}}</p>
    </div>
{{/each}}
