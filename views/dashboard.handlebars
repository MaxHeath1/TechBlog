{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
    {{#each posts as |post|}}
      {{> dashboardDetails post}}
    {{/each}}

</section>
{{/if}}
<div class="btn-div">
  <button type="submit" class="btn"><a class="link" href="/dashboard/new">+ Make A New Post!</a></button>
</div>