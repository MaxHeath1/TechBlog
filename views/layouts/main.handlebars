<!DOCTYPE html>
<html lang='en'>

  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Tech Blog</title>
    <link rel='stylesheet' href='/css/style.css' />
  </head>

  <body>

    <div class='wrapperHeader'>
      <header class='navHeader'>
        <div>
          <nav class='navigation'>
            <a class='linkHeader' href='/'>Home</a>
            <a class='linkHeader' href='/dashboard'>Dashboard</a>
            {{#if loggedIn}}
              <a class='linkHeader' href='/' id='logout'>Logout</a>
            {{else}}
              <a class='linkHeader' href='/login'>Login</a>
            {{/if}}
          </nav>
        </div>
        <h1 class='title'><i class='fab'></i>Max's Tech Blog</h1>
      </header>

      <main>
        {{{body}}}
      </main>
    </div>

    {{#if loggedIn}}
      <script src='/js/logout.js'></script>
    {{/if}}

  </body>

</html>